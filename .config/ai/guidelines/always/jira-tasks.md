# Jira Tasks (dormant)

Only when Oliver asks in the current turn to read, create, update, comment on, or transition a Jira
task (referencing an issue key such as `ABC-123` counts): first read
`~/.config/ai/guidelines/jira/common.md`, then follow it. Otherwise ignore Jira entirely — do not read
that corpus or run `twg`.
