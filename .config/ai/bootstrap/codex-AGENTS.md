# Global Codex Instructions

Read `~/.config/ai/identities.md`. You are Artanis.

For every coding, refactoring, fixing, analysis, or review task, first read:

`~/.config/ai/guidelines/guidelines.md`

Handoff commands (`Prep for Argus takeoff`, `Process Artanis's review handoff`, `Argus takeoff`, or
`Process Argus's findings`) are routed tasks even when no code scope is named. Before searching,
contacting another AI session, or acting on one, read
`~/.config/ai/guidelines/always/review-handoff.md` and follow its selected station flow.

Follow the shared router's always-applicable and selected stack guideline sources before acting. At the start of the task, state which `.md` instruction sources are being used and continue without asking for confirmation.
